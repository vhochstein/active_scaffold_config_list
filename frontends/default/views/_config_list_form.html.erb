<%= render :partial => "base_form", :locals => {:xhr => xhr ||= nil,
                                                :form_action => form_action ||= :config_list,
                                                :method => method ||= :post,
                                                :cancel_link => cancel_link ||= true,
                                                :headline => headline ||= active_scaffold_config.config_list.label,
                                                :body_partial => 'config_list_form_body'} %>
